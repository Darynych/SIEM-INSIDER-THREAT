# docs — diagrams & screenshots

Drop your screenshots in this folder with these **exact filenames** so they render in the main README:

| Filename | What to capture |
|---|---|
| `kibana-rules.png` | Kibana → Security → Rules, showing your insider-threat rules **enabled** with "Succeeded" status |
| `n8n-workflow.png` | The n8n SOAR canvas after a run — the green node chain |
| `n8n-execution.png` | *(optional)* an execution showing the enriched alert (the geo/ISP added to the IP) |
| `ml-demo.png` | *(optional)* the Isolation Forest demo output flagging the planted insiders |
| `architecture.png` | *(optional)* an exported architecture diagram (the README already renders one via Mermaid) |

Tips: crop tightly, use PNG, and aim for readable text. These are the images a reviewer looks at first — they're worth getting clean.
